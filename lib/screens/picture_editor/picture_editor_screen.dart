import 'package:flutter/material.dart';

class PictureEditorScreen extends StatelessWidget {
  const PictureEditorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
